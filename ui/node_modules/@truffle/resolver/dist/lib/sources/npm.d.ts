import { ResolverSource } from "../source";
export declare class NPM implements ResolverSource {
    workingDirectory: string;
    constructor(workingDirectory: string);
    require(importPath: string, searchPath: string): any;
    resolve(import_path: string, _imported_from: string): Promise<{
        body: string;
        filePath: string;
    }>;
    resolveDependencyPath(import_path: string, dependency_path: string): string;
}
