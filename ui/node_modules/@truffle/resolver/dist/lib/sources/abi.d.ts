import { ContractObject } from "@truffle/contract-schema/spec";
import { FS } from "./fs";
export declare class ABI extends FS {
    require(): ContractObject | null;
    resolve(importPath: string, importedFrom?: string): Promise<{
        filePath: string;
        body: string;
    }>;
}
